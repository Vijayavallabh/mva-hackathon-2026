# Track 2 v17 harness review

24 September 2026. Current presentation v17; science v15; feat-009 remains the sole
active feature. This is a software/state review, not scientific or competition scoring.

## Routes and checks

`notes/track2-current.json` identifies report, deck, narration, transcript, video
description, methods exporter, PDF renderer, public reviewer command, requirement
record, community review, owner-readiness note, release and recording-materials bundle.
AGENTS, README, feature evidence and session handoff point to those current paths.
The harness preserves null rescue priority and exposure margin, unconfirmed phase,
no wet-lab/video/upload, unverified provider settings and unresolved licensing scope.

The public reviewer command has no dependency on local historical output archives.
It passed in a clean exported copy with network/protected-path access forbidden:
see `track2-public-review-v17.json`. The stricter research release separately binds all
earlier input hashes and recursively verifies historical snapshots. This distinction
prevents reviewers from needing gated data or unavailable local output folders merely
to inspect the proposal. Neither command establishes biological validity.

Current checks reject false quota or compliance promotion, incomplete discussion/source
coverage, missing AI/methods answers, unqualified scientific summaries, mismatched script
and slides, absent trial uncertainty, misleading synthetic bar scales, missing main
stop-rule labels, unsafe remote assets, changed export sources and overwritten history.
The source-label regression was strengthened after a real mutation exposed a footer
matching the missing primary label. Semantic IDs now identify the six decision labels.

The methods exporter pins the actual organizer workbook and preserves its questions;
the current three-entry rule is explained without silently rewriting that template.
The report uses absolute public links. Final PDF/workbook source hashes and slide
geometry/visual reviews are recorded in the v17 audits. ZIP construction checks every
included export against the audited hashes, verifies CRC/bytes and refuses overwriting.

## Structural skill audit

Command:

```bash
uv run node /home/sports/.agents/skills/harness-creator/scripts/validate-harness.mjs --target /mnt/md0/IITM/BackUp/Home/vijayavallabh/mva-hackathon-2026
```

The validator reports 100/100: instructions, state, verification, scope and lifecycle
each 5/5. It labels instructions the bottleneck only by tie ordering; this is no measured
operational bottleneck and no evidence of better research or a judging score. Reliability
evidence comes from the isolated-copy run, meaningful mutation tests, actual exports,
historical verification and fresh-shell startup, whose final outputs are in session 56
of `progress.md`.

No new dependencies enter the project lockfile; the document exporter pins transient
dependencies in its uv script header. No subject data, GPU model run, provider-policy
attestation, organizer message, portal callback or upload is part of these checks.
Future changes require a new immutable revision; do not patch old bound files to make
verification pass. Remaining delivery items are explicit, separate from biological gaps.
