# V14 integration and presentation review

Session 54, 2026-09-21. Owner requested slides, video transcript, related files and
especially the harness. Preserve v13 and every earlier bound input.

Design plan: retain the reviewed paper #f6f7fb, ink #241d32, plum #634285,
teal #17666c, pale plum #eae3f2 and night #30203e palette. Liberation Serif carries
questions; Nimbus Sans carries evidence at a 24px minimum. Keep source links,
keyboard focus, network-free rendering and the reduced-width view.

Use nine slides. Split the former model page into two: sequence-control successes
alongside earlier disagreement, followed by structural and DNA-model limits.
Two comparative rows make the change in sequence evidence readable without a
misleading cross-model effect-size chart. Three rows on the next slide preserve
the impaired structural controls, ESM3 WT variability and BRCA1 transfer limit.
This comparison layout serves the scientific argument; decorative model logos
or GPU counts would compete with it. Keep the existing seven narrative pages,
including the exact ARST1431 plot and complete acknowledgement.

The intended takeaway is stronger computational motivation to test N1002K,
with no functional validation, drug ranking, phase or exposure promotion. All
model counts refer to this follow-up, not new biological experiments. The drug
science in report sections 2–7 is unchanged. The transcript contains 336 words
across nine aligned paragraphs; timings are allocations and runtime is unmeasured.

AlphaFold3-derived findings carry a conspicuous slide-3 terms/modification notice,
a primary-paper citation and links to the exact terms/notice. The renderer and
release package carry both accompanying files. The report and video description
repeat the disclosure. No new model provider or biological inference was invoked.

Author visual/source review is complete. All nine PDF pages were rasterized and
inspected; the two new model pages were also inspected at full size. The first
render passed: zero clipped text or overlaps, minimum 24px, and 640px viewport with
no horizontal overflow. Checked text contrast is at least 5.43:1. The deck contains
731 visible words including sources/full acknowledgement, and eight SVG figures
(seven conceptual comparisons/schematics and the unchanged published trial plot).
The 261-word methods abstract remains below the organizer's 500-word ceiling.

Artifacts: `results/feat009/v14-slide-review-first-20260921/`, including nine PNGs,
nine rasterized PDF pages, contact sheet, PDF, render manifest and accompanying
AlphaFold3 terms/notice. `notes/track2-v14-render-audit.json` binds source/renderer
and output hashes, layout checks and author review. The scientific validation
binds that audited source before release. No new model inference, protected-input
processing or independent scientific review occurred. Runtime remains unmeasured.

The harness review is recorded separately in `track2-harness-review-v14.md`.
A regression caught and corrected an overly broad stale-version check: the current
presentation is v14 while the unchanged drug ledger is legitimately v10. Both
versions now have explicit roles rather than being forced to one number.
